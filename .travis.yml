language: node_js
node_js:
  - "node"
install:
  - npm i -g npm
  - npm ci
script:
  - npm run standard
  - npm run flow
deploy:
  provider: npm
  email: bret.maxime@gmail.com
  api_key: $NPM_TOKEN
  on:
    tags: true