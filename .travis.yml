language: node_js
node_js:
  - "node"
install:
  - npm i -g npm
  - npm ci
script:
  - npm run standard
  - npm run flow
branches:
  only:
    - master
    - next
cache:
  directories:
    - "$HOME/.npm"